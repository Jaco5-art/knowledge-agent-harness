# 求职表述

## 项目名称
证据约束的多智能体知识抽取与 Agent Harness

## 简历参考
- 基于 Python、OpenAI API 与 FAISS 构建关系抽取、检索验证和纠错流程；在固定 100 篇 Re-DocRED 子集上，原版 RAG 验证精确率由 46.92% 提升至 48.82%，误报由 560 降至 520，F1 由 22.66% 提升至 22.91%。
- 后续实现 SQLite 会话持久化、类型化工具注册、调用预算、重试回退和人工复核，接入 LangGraph / OpenAI Agents SDK 可切换调度及 MCP stdio 只读查询；完成故障恢复和运行时一致性验证。
- 构建后置语义门控与可观测实验流程；10 篇合成扩展文档中拦截 1 条歧义实体关系；6 个上下文验证案例输入 token 减少 42.26%，同时记录否定判断波动，未宣称质量无损。

简历篇幅有限可只用前两条。原版效果实验与后续工程升级必须在时间与范围上分开，不写“升级版准确率 100%”。

## 约一分钟介绍
项目最初研究文档中的关系抽取和证据验证，在固定 100 篇公开基准上比较基线、验证和 RAG。结果是精确率有小幅提升，但召回率仍低。之后我把执行流程升级成 Harness，增加持久化、重试、预算、人工复核、语义门控和 MCP 查询，并让 LangGraph 与 Agents SDK 共用同一套业务规则。工程能力通过离线故障测试，部分机制做了真实 API 对照。升级后没有再跑完整 100 篇，所以我分别报告原基准成绩与升级专项结果，也保留否定判断错误等局限。

## 常见追问

**为什么事实明确，模型仍会错？**
我们观测到证据明确否定收购，但模型把正向候选误读成负向候选并判支持。结构化输出限制格式，不能保证语义正确。

**这是自主 Agent 吗？**
目前两个框架的调度均由可信状态确定，工具执行与业务规则受控；没有证明模型能自主规划任意任务。

**生产级吗？**
它是采用生产工程思路的个人项目，具备状态、边界和观测机制；没有开展大规模压测、远程认证或真实线上 SLO 验证。

**为什么只测了 10 篇？**
10 篇针对特定门控机制，不替代原 100 篇基准。它展示具体错误拦截，不能估计一般业务准确率。

**MCP 做了什么？**
通过本地 stdio 将已有只读历史查询暴露为工具，作用域由服务启动参数固定；客户端不能通过工具参数切换租户或传入 SQL。不是仅安装依赖，也不是远程多租户认证系统。

**100 篇是 2023 年之前吗？**
未逐篇核实。文档年代、事件年份、数据集版本不同；公开基准也不能排除训练重叠。不要给未经核实的年份保证。
