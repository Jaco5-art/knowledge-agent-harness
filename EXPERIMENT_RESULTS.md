# 实验结果与证据边界

## 原版：固定 100 篇 Re-DocRED

| 条件 | Precision | Recall | F1 | TP | FP | FN |
|---|---:|---:|---:|---:|---:|---:|
| 基线抽取 | 46.92% | 14.94% | 22.66% | 495 | 560 | 2819 |
| 仅验证 | 48.32% | 14.79% | 22.64% | 490 | 524 | 2824 |
| RAG 验证 | 48.82% | 14.97% | 22.91% | 496 | 520 | 2818 |

RAG 相对基线：精确率增加 1.90 个百分点，F1 增加 0.25 个百分点，净误报减少 40 条。不是“F1 大幅提升”。Evidence Recall@3 = 1283/1424 = 90.10%，是有证据标注关系上的检索覆盖率，不是抽取准确率。

配置：gpt-5-mini、text-embedding-3-small、top_k=3、chunk_size=350、overlap=75。固定子集包含 3314 条 gold 关系、96 类关系词表。原始报告、预测及运行配置见 evidence/original_100/。兼容审计复现基线分数；未调用模型。

这些成绩属于原流程。Re-DocRED 是公开基准，未逐篇核实原文年代或排除训练数据重叠，不宣称全新资料泛化。

## 升级版：后置语义门控

固定合成扩展集 10 篇（5 组 clean/noisy 配对），同一上游会话回放两条输出路径：

| 指标 | 门控关 | 门控开 |
|---|---:|---:|
| 完全匹配文档 | 9/10 | 10/10 |
| 关系 TP / FP / FN | 5 / 1 / 0 | 5 / 0 / 0 |
| noisy F1 | 0.80 | 1.00 |
| noisy 复核判断正确率 | 80% | 100% |

改变集中于 identity_noisy：Aster 对应两个实体，门控阻止未确定身份的投资关系发布。其余九篇保持一致。完全匹配包含预测集合、复核判断和重复输出要求；不等于独立人工证据蕴含评分。

两条路径共享谓词规范化、去重、上游身份上下文；这是后置目录门控的消融，不是整个语义系统开/关。样本实体和场景与开发阶段相近，是合成扩展集而非盲测。不同历史批次不合并计算。

## 升级版：上下文压缩

6 个合成验证案例，每种模式各 6 次真实请求：

| 指标 | 未压缩 budget | compact |
|---|---:|---:|
| 正确接受/拒绝 | 6/6 | 5/6 |
| 输入 token | 10732 | 6197 |
| 输出 token | 2939 | 4015 |
| 总 token | 13671 | 10212 |

输入减少 42.26%，总 token 减少 25.30%；输出 token 增加。usage 记录覆盖 12/12。所有 payload 无损检查通过。无重叠对照两种输入都是 935 token。错误是将被否定的收购关系误判为支持。

同一否定案例再固定输入重复 6 轮：未压缩正确 3/6、压缩正确 5/6；两种模式均存在错误。输入分别 11694/6288，总 token 15499/10824。该诊断不能证明压缩更准确，也不能确定首次错误由压缩导致。压缩保持可选，不宣称质量无损。两次实验单列，不合并成通用准确率。

## 工程验证

- 故障恢复：7 个场景、9 项检查通过；正常 9 次调用，临时错误重试恢复 10 次；空检索全文回退后仍验证，最终事实一致；永久错误不重试。
- LangGraph 与 Agents SDK：8 次离线运行、10 项检查通过；共享业务策略、SQLite 和确定性工具，实际调用 StateGraph.ainvoke 与 Runner.run。
- 统一入口：三种 runtime 均支持 CLI 创建、暂停恢复、完成会话重复调用不增加次数；本轮每个 runtime 11 次调用。与前一组 9 次调用使用的配置不同，不能当作性能差异。
- MCP：2.2.0，真实本地 stdio 服务端/客户端，9 项检查通过；作用域、参数、只读和句柄关闭检查。
- 整体回归：用户 Windows 环境 160 passed, 3 subtests passed，6.65 秒。该回归发生在统一 runtime 入口及 MCP 新增前；后两者另有上述专项验收，不声称全量测试覆盖所有后续代码。

工程测试不衡量模型准确率、真实线上恢复率或性能排名。所有实验原始报告见 evidence/。

## 未执行

升级版 Harness 的原固定 100 篇真实评估未执行；没有升级版对应 P/R/F1。新编号适配器只完成离线约束、纠错再验证、导出测试。不得重命名原预测来冒充新运行。
