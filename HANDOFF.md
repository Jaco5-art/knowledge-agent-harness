# 收尾与交接

本次决定：停止新增付费实验，归档现有实现与证据。当前 Windows 项目文件夹仍是使用中的工作副本；本包是根据上传源码和已交付修改重建的可审查快照，不含用户本地所有会话数据库、凭证或虚拟环境。

## 已交付范围

Harness 主流程与业务约束；上下文预算和压缩；语义门控；历史只读查询；可观测日志与实验；LangGraph/SDK 调度对照及统一入口；MCP 本地工具接入；Re-DocRED 编号适配层。

## 未交付或未验证

- 升级版 100 篇在线评估及其批量执行入口：按用户决定停止。
- 原生 LangGraph checkpoint 与 SDK session 的比较、进程崩溃恢复：未验证。
- 自主 LLM 调度、远程 MCP 认证、多用户服务、通用执行沙箱：不在本次已交付范围。
- MCP 调用自动进入模型工具选择循环：未实现；当前为明确的客户端/服务端命令。
- 原版低召回与模型否定误判：已知局限，未宣称修复。

## 版本与复现

1. 保留现有 v0.3.1 工作文件夹；不要把整个包覆盖到仍在恢复的实验目录。
2. 包中源码已含角色预算修复和统一运行时入口，测试夹具来自原项目副本；原始业务/实验输出未改写。
3. 原来的版本字符串可能仍显示 0.2.0：这是历史包元数据，不能作为实验代码标识。以归档 manifest 的 SHA256 与各报告指纹区分实际代码。
4. 从 evidence 报告生成时到预算修复、CLI/MCP 增补存在代码变化；报告说明当时版本，不代表最终快照每项能力都经过同一轮测试。
5. 某些业务批次恢复时检查全部 Harness 源码指纹。后续源码变化后，旧批次可能拒绝继续，这是保护措施；不要删除指纹或伪造旧版本。
6. 正常演示无需 API；包含 --execute 的历史复现实验会付费。本次不需要执行。
7. 不含 API key、.env、.venv 或个人业务数据库。本包中 data/ 为原固定公开基准子集与标签；gold 数据仅供评分，不得传入模型提示。
8. Python 3.12 为已验证环境；未声称整个 MCP/SDK 组合兼容项目元数据声明的全部 Python 版本。

## 证据保留

evidence/original_100/ 保存原预测、配置、评分和 trace；其余为用户上传的新实验和 Windows 验收。新编号适配层验收来自用户反馈 INDEXED ADAPTER OK，未伪造对应在线报告。

## 演示顺序

离线 demo → 统一 runtime 帮助和选择 → 会话/trace 展示 → MCP self-test → 解释原基准与专项实验 → 展示已知失败案例。

无需新增实验即可完成这次交付。后续若继续研发，再单独启动版本与评估周期。
